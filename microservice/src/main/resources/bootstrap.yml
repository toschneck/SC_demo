debug: true

spring:
  application:
    name: foo
  cloud:
    config:
      failFast: true
      discovery:
        enabled: true
      uri: http://localhost:8888
      username: demo
      password: test

#
#eureka:
#  instance:
#    client:
#      serviceUrl:
#        defaultZone: http://localhost:8761


#      username: user
#      password: consol
encrypt:
  keyStore:
    location: classpath:sc-keystore.jks
#    don't use a default in production
    password: consol
    alias: demokeystore